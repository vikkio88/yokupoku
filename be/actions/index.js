const misc = require('./misc');
const reviews = require('./reviews');
const products = require('./products');
const providers = require('./providers');

module.exports = {
    misc, reviews, products, providers
};
