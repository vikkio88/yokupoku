{
  "name": "yokupoku",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cheerio": "^0.22.35",
    "cheerio": "^1.0.0",
    "imgbox-js": "^1.0.35"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "website/",
    "be/",
    "yokupoku-shared/"
  ],
  "scripts": {
    "be": "cd be && bun start",
    "be:stop": "cd be && bun stop",
    "web": "bun run --filter '*' web:dev",
    "web:clean": "cd website && rm -rf dist",
    "web:build": "cd website && bun run build",
    "game:grab": "bun scripts/gameGrab.ts",
    "games:import": "bun scripts/gamesImport.ts"
  }
}
